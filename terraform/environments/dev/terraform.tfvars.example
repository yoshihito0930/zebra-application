# Dev環境の変数値サンプル
# 実際の値を設定して terraform.tfvars にコピーしてください

environment          = "dev"
aws_region           = "ap-northeast-1"
lambda_artifacts_dir = "../../../build/lambda"

# アラーム通知先メールアドレス（設定すると、アラーム発生時にメールが届きます）
alarm_email = "your-email@example.com"
